## What

## Why

## How I tested it
- [ ] `cd server && uv run bot.py` boots with no tracebacks
- [ ] `cd web && npm run build` passes
- [ ] Tried it by voice (or via `?mock=1` for UI-only changes)

## Screenshots / recording (UI changes)
