[project]
name = "sayso-server"
version = "0.1.0"
description = "Sayso — a voice-native developer agent that operates your terminal, browser and GitHub (Pipecat · SambaNova · Hume)"
authors = [
    { name = "V N Moorthy", email = "vnarasingamoorthy@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "pipecat-ai[deepgram,gradium,hume,local-smart-turn,mlx-whisper,openai,runner,sambanova,silero,webrtc,whisper]>=1.11.0",
    "python-dotenv>=1.2.3",
    "websockets>=17.1",
]
